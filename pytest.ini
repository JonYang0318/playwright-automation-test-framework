[pytest]
testpaths = tests
addopts = -ra
markers =
    e2e: end-to-end tests
    functional: functional tests
    describe(name): descriptive label for test grouping
    smoke: smoke tests
    login: login-related tests
    negative: negative tests
